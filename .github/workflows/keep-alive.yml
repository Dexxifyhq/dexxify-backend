name: Keep Alive

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping server
        run: curl --fail --silent --show-error "${{ secrets.RENDER_URL }}/api/v1/health"
